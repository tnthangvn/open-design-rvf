1. chèn thêm lọc theo tỉnh thành phố, xã phương. (dạng dropdlow select, có chọn tất cả)
2, thêm phần chọn tất cả cho danh sách các sân trên table
3, Phần map thì nên thay đổi theo dạng lấy theo địa chỉ thay vì lấy theo toạ độ hiện tại (Sẽ khó cho ban tổ chức nhập dữ liệu ban đầu, và cho phép chỉnh sửa lại vị trí nếu bị lệch).
4. sau khi update xong thì update lại template [manage-location-clusters-template](../../assets/manage-location-clusters-template.xlsx) để maping với nhau